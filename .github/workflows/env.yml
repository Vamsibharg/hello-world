name: env_var
on:
 workflow_dispatch:
jobs:
  display:
    runs-on: windows-latest
    steps:
      - name: Display custom environment variables
        run: |
          echo "The platform is ${{secrets.API_KEY}}"
          echo "the region is $secrets.API_KEY"
  
 
